apiVersion: v1
kind: Service
metadata:
  name: {{ include "aventus-exercise.fullname" . }}
  labels:
    {{- include "aventus-exercise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aventus-exercise.selectorLabels" . | nindent 4 }}
